#include <iostream>
#include <vector>
#include <numeric>
#include "include/busca.hpp"
#include "include/melhor_caso.hpp"
#include "include/contagem.hpp"

int main() {
    std::vector<int> fila(201);
    std::iota(fila.begin(), fila.end(), -100);
}